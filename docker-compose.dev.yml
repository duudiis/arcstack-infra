services:
  traefik:
    volumes:
      - ./traefik/traefik.dev.yml:/etc/traefik/traefik.yml:ro

  frontend:
    build:
      target: deps
    volumes:
      - ../arcstack-web/src:/app/src
      - ../arcstack-web/public:/app/public
    command: npm run dev
    environment:
      - NEXT_PUBLIC_API_URL=http://api.arcstack.localhost:1300
      - NEXT_PUBLIC_WS_URL=ws://api.arcstack.localhost:1300

  backend:
    volumes:
      - ../arcstack-api/src:/app/src
    command: npx tsx watch src/index.ts
    environment:
      - NODE_ENV=development
      - FRONTEND_URL=http://arcstack.localhost:1300
      - COOKIE_DOMAIN=localhost

  postgres:
    ports:
      - "1310:5432"

  redis:
    ports:
      - "1311:6379"
